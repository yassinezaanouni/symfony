<form method="post" action="{{ path('app_prd_delete', {'id': prd.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prd.id) }}">
    <button class="btn">Delete</button>
</form>
